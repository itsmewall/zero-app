name: Clínica
entities:
  Task:
    labels:
      singular: "Atendimento"
      plural: "Atendimentos"
    fields:
      - key: paciente
        type: string
      - key: procedimento
        type: string
      - key: sala
        type: string
      - key: no_show
        type: boolean
        default: false
workflows:
  Task:
    states: [Agendado, "Em atendimento", Finalizado, Faturado]
    initial: Agendado
